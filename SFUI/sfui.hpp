#include "SFML/Graphics.hpp"
#include <iostream>
#include <string>

using namespace std;

int makeButton(int x, int y) {
    sf::RectangleShape button;
    sf::Text buttonText;
    button.setSize(sf::Vector2f(25, 5));
    button.setFillColor(sf::Color::Blue);
}